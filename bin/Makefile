TARGET_LIST=accountserver  chalserver  clanserver  dbserver  fightServer  friendserver  gameserver  gateserver  gmserver  jfserver  leagueserver  lockserver  mailserver  matchserver  rankserver  tlogserver  tpserver  tttserver robot1

all:
	echo done!

clean:
	@for i in $(TARGET_LIST); do\
		rm $$i;\
	done;
